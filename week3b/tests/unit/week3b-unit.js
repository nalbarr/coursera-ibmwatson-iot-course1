/* eslint no-process-env:0 */
import {expect} from 'chai';
import {describe, it} from 'mocha';
import {week3blogic} from '../../src/domain';
describe('foo', () => {
    it('should return 1', function(){
        var f = foo(1);
        assert.equal(f, 1);
    });
});